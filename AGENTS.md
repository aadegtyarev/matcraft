<!-- ai-dev:breadcrumb -->
This project runs the **ai-dev protocol**; its active platform is **claude** — this OpenCode session has no protocol wiring yet.
Run `node .ai-dev/tooling/src/adapter/install.mjs . --platform opencode` to wire OpenCode, then offer the Operator the platform switch (`.ai-dev/tooling/src/agents/orchestrator.md` `## Setup`, "Platform switch").
<!-- /ai-dev:breadcrumb -->
